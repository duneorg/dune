/**
 * Routing module — URL resolution and Fresh route registration.
 */

export { createRouteResolver } from "./resolver.ts";
export type { RouteMatch, RouteResolver, RouteResolverOptions } from "./resolver.ts";
export { duneRoutes } from "./routes.ts";
